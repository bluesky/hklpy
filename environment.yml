name: hklpy
channels:
  - nsls2forge
  - conda-forge
  - defaults
dependencies:
  - python>=3.6
  - pip
  - hkl
  - ipython
  - numpy
  - ophyd
  - pint
  - prettytable
  - pip:
    - coveralls
    - pytest
    - pytest-cov
